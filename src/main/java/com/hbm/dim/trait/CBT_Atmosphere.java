package com.hbm.dim.trait;

import com.hbm.inventory.fluid.FluidType;

// Stub — always returns false until atmosphere system is ported
public class CBT_Atmosphere {
    public boolean hasFluid(FluidType fluid, double min) {
        return false;
    }
}
