package com.hbm.lib;

//~ stealth-todo: Bob's Library is a util namespace class (~300+ static methods);
//~ context: we stub only enum constants used by BlockBarrier — ordinals 0..5 must match Bob's ForgeDirection ordinals (DOWN/UP/NORTH/SOUTH/WEST/EAST)
public enum Library {
    NEG_Y, POS_Y, NEG_Z, POS_Z, NEG_X, POS_X;
}
