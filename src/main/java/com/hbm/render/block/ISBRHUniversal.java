package com.hbm.render.block;

//~ stealth-todo: full Bob's ISBRH render system not portable to 1.12.2 (1.12 uses IBakedModel);
//~ context: marker interface only — renderInventoryBlock/renderWorldBlock methods deferred to render phase
public interface ISBRHUniversal {
    // marker interface — full impl deferred to render phase
}
